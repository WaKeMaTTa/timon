.card
  .card-header
    %h2 Users

  .card-content
    .table-responsive
      %table.table.table-hover.table-striped
        %thead
          %tr
            %th Name
            %th Email
            %th Role
            %th Delete?

        %tbody
          - @users.each do |user|
            = render user
