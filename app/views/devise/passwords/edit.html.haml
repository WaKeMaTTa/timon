.recover-password
  .inner-content
    .card
      .card-title
        %h2.title Change your password

      .form-container.no-border
        = form_for(resource, as: resource_name, url: password_path(resource_name), html: {role: 'form', method: :put}) do |f|

          %p We'll send password reset instructions.

          = devise_error_messages!

          = f.hidden_field :reset_password_token

          .form-group
            = f.label :password, 'New password'
            = f.password_field :password, autofocus: true, autocomplete: 'off', class: 'form-control'

          .form-group
            = f.label :password_confirmation, 'Confirm new password'
            = f.password_field :password_confirmation, autocomplete: 'off', class: 'form-control'

          = f.submit 'Change my Password', class: 'btn btn-block btn-success submit'
